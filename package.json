{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:static": "nuxt generate",
    "deploy": "npm run build:static",
    "generate-sitemap": "node scripts/generate-sitemap.js",
    "update-sitemap": "node scripts/update-sitemap.js",
    "pre-deploy": "npm run generate-sitemap && node scripts/pre-deploy.js",
    "post-deploy": "node scripts/post-deploy.js",
    "test-deployment": "node scripts/test-deployment.js"
  },
  "dependencies": {
    "nuxt": "^3.17.6",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1",
    "aos": "^2.3.4"
  }
}
